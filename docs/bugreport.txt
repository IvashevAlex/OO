В тестовой версии 1.0.0.8 при попытке удалить через меню админа юзера с id 5484457194 в терминале
выходит ошибка list index out of range и удаление не происходит, хотя сообщение об удалении происходит
        Ошибка была связана с измененим структуры БД. Запрос на удаление ссылался на несуществующую таблицу

При создании новой БД SSMS на сервере не смог автоматически распознать строку UserMark как битовую (логическую)
        Ситуация разрешилась указанием типа данных как tinyint, а после создания конвертация данных в bit через команду
        ALTER TABLE  WhiteList ALTER COLUMN UserMark bit;
        
При создании рассылки функции расположенные в main запускаются только  один раз. Далее запускается 
цикл бесконечного пуллинга , которы не прирывается.
        Использовать прерывающийся пуллинг, Запуск двух параллельных агентов, Разбиение на потоки
        Решено запуском папллельного процесса в другом файле обрабатывающим рассылку

В рамках версии 1.0.1.7 был переннос SQL команд в отдельны файл. Это привело к ошибкам во взаимодействии с БД
        Принято решение откатить изменения и не вносить данную правку, а продолжить работу с рассылкой

Не получается получить введенный текст в качестве аргумента для добавления информации в базу данных. Для обработки используется два 
декоратора message_handler и callback_query_handler. Каждый из них можно запустить по отдельности. Первый получает текст сообщения,
и его можно вывести на экран, но не получается сохранить без использования глобальных переменных. Второй запускаеи функцию при 
нажатии кнопки в меню, котора активирует запись в БД. Проблема в том, что в callback_handler не попадает информация из сообщения, а
полученные данные в message_handler к callback_query_handler приходят уже пустыми. 
Возможно нужно использовать праильную архетиктуру или есть другой подходящий для данного случая декоратора
        Попробовать использовать bot.register_next_step_handler(message.message, name_func)
        Реализация удалась. Запись по кнопке работает. Несколько неудобно каждый раз заново запрашивать /admin заново

При подготовке тестовой рассылки обнаружено, что диапазоны дат пересекаются и рассылка попадает сразу на две группы. Первый день 
новой группы - он же последний день старой группы
        Решается исправлением в make_lists_of_dates

Предпологается, что прежний фикс с датами вызовет ситуацию с отрицательным диапазоном дат


Если диапазон дат превышает данные в календаре дни, то возникает ошибка. 
Когда набор 20.09.2022 выйдет за рамки скорее всего будет ошибка


Если юзер блокирует бота и мы пытаемся отправить ему сообщания возникает ошибка 
        Исправлено добавлением блока try-except

Разница меджу датами 20.09 и 22.09 считается как 2 дня, хотя по факту это 3.
        Временно сдвинул календарь на день. Буду добавлять в логику +1 и тестить.
        